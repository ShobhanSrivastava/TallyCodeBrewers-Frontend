import { useNavigate, Link } from 'react-router-dom';

function Start() {
    // const navigate = useNavigate();
    return (
        <>
            Hello
            <Link to='/'>Solo Mode</Link>
        </>
    );
}

export default Start;